@PACKAGE_INIT@

# avoid repeatedly including the targets
if(NOT TARGET cpp-ap::cpp-ap)
    # Provide path for scripts
    list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")

    include(${CMAKE_CURRENT_LIST_DIR}/cpp-ap-targets.cmake)
endif()
